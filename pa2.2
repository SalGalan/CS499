
import requests
import csv

pullRequest = 0
  
counter =0
data = NULL 
url = "https://api.github.com/repos?page=1&page=2&per_page=50/rails/rails/pulls/'number'"
pullRequest(0: 50)


for commit in commits:
    if commit.commit is "Thanks" or "Thank You" or "Tks":
      counter ++
        print counter
        
with open('data.csv', 'w') as f:
    writer = csv.writer(f)
    writer.writerow(data(row))
